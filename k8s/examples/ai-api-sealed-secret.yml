apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ai-api-secret
spec:
  encryptedData:
    OPENAI_API_KEY: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: ai-api-secret
    type: Opaque
